#homework Домашка.
1. Выучите все термины, которые мы прошли на прошлом уроке. Загуглите определения тех терминов, которые вы не понимаете, или на которые я не дал определения.
2.
Создайте новый проект js_front_04. Далее я не буду повторять процесс создания нового проекта, его базовой структуры и скрипта, это подразумевается. Далее скрипт:
3.
Создайте 5 объектов, назовём их - герои.
Каждому герою назначьте свойства: имя (строка), возраст (число), имя питомца (pet, строка). Придумайте любые данные.
Создайте массив heroes, который содержит этих героев.
4.
Создайте функцию getPetName, которая принимает аргумент index (индекс персонажа в массиве) и возвращает имя питомца этого персонажа (подсказка: внутри цикла используйте условие и оператор return).
Вызовите эту функцию 3 раза с любыми значениями параметра, выведите результаты в консоль. Исправьте ошибки.
5.
Создайте функцию getBirthYear, которая возвращает год, в котором родился герой.
Вызовите эту функцию 3 раза с любыми значениями, результат выведите в консоль. Исправьте ошибки.
6.
Создайте функцию findOlder, которая принимает 2 параметра: a, b - индексы героев в массиве, и возвращает имя героя, чей возраст больше.
Вызовите эту функцию 3 раза с любыми значениями. Проверьте правильность работы функции, исправьте ошибки.
7.
Придумайте несколько функций, которые бы можно было использовать в жизни и/или в разработке, напишите код этих функций, проверьте их работоспособность.
8.
Скиньте архив проекта через гдиск.
Выполните это задание до понедельника.